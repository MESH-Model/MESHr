% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tb02dataframe.R
\name{tb02dataframe}
\alias{tb02dataframe}
\title{Reads in a ts0 file}
\usage{
tb02dataframe(tb0File = "", values_only = TRUE, timezone = "",
  NAvalue = NULL)
}
\arguments{
\item{tb0File}{Required. The name of the file to be read.}

\item{values_only}{Optional. If \code{TRUE}, only the time series values will
be returned. If {FALSE}, the meta data will also be returned. Note that if
the value is set to \code{TRUE}, and there are no time series values in the
file, as for a reservoir file, then an error will result.}

\item{timezone}{Optional. The data time series have \code{POSIXct} datetime
values. You may want to specify the \code{timezone} of the data.
If the \code{timezone} is not specified, your default value (i.e. your
time zone) will be used.}

\item{NAvalue}{Optional. If specified, values smaller than \code{NAvalue}
will be set to \code{NA_real_}}
}
\value{
Returns time series as a data frame. If meta data are specified, they
are returned as lists.
}
\description{
Reads in a ts0 file. The time series values, if present, will be
stored in a data frame. Optionally, the meta data will be stored as lists. 
The meta data are of 2 types, the header values, which refer to the entire 
file and column values, which pertain to individual columns.
}
\examples{
\dontrun{
qvals <- tb02dataframe("MESH_input_streamflow.tb0", NAvalue = -0.01, values_only = TRUE)
}
}
\seealso{
\code{\link{r2c2raster}} \code{\link{readOutputTimeseriesCSV}}
}
\author{
Kevin Shook
}
